# Browser-facing API base URL.
# Empty means same-origin `/api`, which works in production behind nginx and in dev through Vite proxy.
VITE_API_BASE_URL=

# Vite dev proxy target for same-origin local requests.
VITE_PROXY_API_TARGET=http://localhost:8080
